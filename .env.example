# Copy to .env and fill in. .env is gitignored and must never be committed.
#
# Get a key at https://console.getsolari.com — label it `coldstart-dev`.
# One key works across browsers, sandboxes, and desktops.
SOLARI_API_KEY=slr_live_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
